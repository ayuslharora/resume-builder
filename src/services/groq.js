// Compatibility shim for tests and imports that still expect this module.
// The real implementation lives in `llm.js`.
// Candidate's raw background/source document
// Relevant source document context
// targetContext.sourceDocumentText

export * from './llm.js';
